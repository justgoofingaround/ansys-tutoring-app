# Tutoring Hub server dependencies (webapp builds separately via npm).
# The ollama client is listed so the NYU deployment can reach its local
# Ollama container (report review, FAQ drafting, PDF->tutorial); it is inert
# on cloud deployments, which run with ENABLE_LLM=0. The Compass retrieval
# extras (chromadb, sentence-transformers) are still NOT listed; see
# chatbot_spike/requirements.txt for a full local install.
fastapi>=0.115,<1
uvicorn>=0.30,<1
bcrypt>=4.0,<5
python-multipart>=0.0.9,<1
pymupdf>=1.24,<2
# Cloud chatbot engine (CHATBOT_API_KEY set): OpenAI-compatible streaming.
httpx>=0.27,<1
# Local LLM client (talks to Ollama at OLLAMA_HOST).
ollama>=0.4,<1
