# Tutoring Hub server dependencies (webapp builds separately via npm).
# LLM extras (ollama, chromadb, sentence-transformers) are NOT listed:
# cloud deployments run with ENABLE_LLM=0 and every LLM code path imports
# lazily and degrades gracefully. For a full local install see
# chatbot_spike/requirements.txt as well.
fastapi>=0.115,<1
uvicorn>=0.30,<1
bcrypt>=4.0,<5
python-multipart>=0.0.9,<1
pymupdf>=1.24,<2
# Cloud chatbot engine (CHATBOT_API_KEY set): OpenAI-compatible streaming.
httpx>=0.27,<1
